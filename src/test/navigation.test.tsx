import { describe, it, expect } from 'vitest'

describe('Navigation', () => {
  it('Given page loads, When I check navigation, Then it renders without error', () => {
    // Given
    expect(true).toBe(true)
  })
})