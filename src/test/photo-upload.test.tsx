import { describe, it, expect } from 'vitest'

describe('Photo Upload', () => {
  it('Given photo is uploaded, When saved, Then it has required fields', () => {
    // Given
    expect(true).toBe(true)
  })
})